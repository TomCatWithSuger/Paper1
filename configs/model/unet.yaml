_target_: src.models.unet_module.UNetLitModule

optimizer:
  _target_: torch.optim.Adam
  _partial_: true
  lr: 0.0002
  weight_decay: 0.0

scheduler:
  _target_: torch.optim.lr_scheduler.ReduceLROnPlateau
  _partial_: true
  mode: min
  factor: 0.5
  patience: 10

net:
  _target_: src.models.components.unet_1d.UNet1D
  in_channels: 1
  out_channels: 1
  channels: [8, 16, 32]
  kernel_size: 5

compile: false
