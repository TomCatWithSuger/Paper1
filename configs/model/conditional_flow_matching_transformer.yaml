_target_: src.models.conditional_flow_matching_module.ConditionalFlowMatchingLitModule

optimizer:
  _target_: torch.optim.Adam
  _partial_: true
  lr: 0.0002

scheduler: null

condition_encoder:
  _target_: src.models.components.meanvc_conditioning.CachedConditionEncoder
  content_dim: 256
  speaker_dim: 256
  output_dim: 128

net:
  _target_: src.models.components.conditional_flow_matching.ConditionalFlowMatchingTransformer
  n_mels: 80
  hidden_dim: 256
  condition_dim: 128
  num_layers: 6
  num_heads: 8
  integration_steps: 50

compile: false
