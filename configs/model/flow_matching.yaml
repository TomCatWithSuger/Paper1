_target_: src.models.flow_matching_module.FlowMatchingLitModule

optimizer:
  _target_: torch.optim.Adam
  _partial_: true
  lr: 0.0002
  weight_decay: 0.0

scheduler:
  _target_: torch.optim.lr_scheduler.ReduceLROnPlateau
  _partial_: true
  mode: min
  factor: 0.5
  patience: 10

net:
  _target_: src.models.components.dense_flow_matching.DenseFlowMatching
  input_shape: [1, 28, 28]
  hidden_dims: [512, 512, 256]
  time_embedding_dim: 64
  integration_steps: 100

compile: false
