# @package _global_

defaults:
  - override /data: atcosim
  - override /model: unet
  - override /callbacks: default
  - override /trainer: default

tags: ["atcosim", "unet", "waveform-reconstruction"]

seed: 12345

data:
  batch_size: 4

trainer:
  min_epochs: 1
  max_epochs: 20
  gradient_clip_val: 1.0

model:
  compile: false
